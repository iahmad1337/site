- Simple static site on jquery
- Don't know If I should write my own server or serve it using already existing
  ones (like nginx)
- Should be containerized
- TODO: base64 encode the favicon and display it on site
