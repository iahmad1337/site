FROM ubuntu:22.04

################################################################################
#                                  ATTENTION                                   #
################################################################################
# This is just a stub

# WORKDIR /app

# COPY . /app

# RUN chmod +x ./generate_build.sh

# RUN ./generate_build.sh release

# RUN curl -sSL https://install.python-poetry.org | python3 -
# ENV PATH="/root/.local/bin:$PATH"
# RUN poetry install --no-root

# ENV ENVIRONMENT=${ENVIRONMENT}
# CMD ["./start.sh"]
